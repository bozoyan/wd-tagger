cd /Volumes/AI/AI/Whisper_Input
--------
git init                             # 初始化本地 Git 仓库
git remote add origin https://github.com/bozoyan/Whisper_Input.git
git add .                            # 添加所有文件
git commit -m "Initial commit"       # 提交到本地仓库
git branch -M main                   # 设置分支为 main（GitHub 默认是 main）
# 拉取远程的 main 分支内容并自动合并
git pull origin main --allow-unrelated-histories
git push -u origin main             # 推送到远程 GitHub 仓库
git push --force origin main    #强制覆盖
---------

git add .                   
git commit -m "1.3.1 说明文档更新"
git push

--------

审阅@app.py 这份代码，帮在@README.md里面写一份中文说明文档。不需要管 │
│    @requirements.txt 或者其他文件，只需要看@app.py的代码就可以。